services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: beerpong
      POSTGRES_USER: beerpong
      POSTGRES_PASSWORD: beerpong_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
